# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse

if TYPE_CHECKING:
    from .client import Client
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class PatientActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Patient]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Patient.prisma().query_raw(
            'SELECT * FROM Patient WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Patient
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Patient.prisma().query_first(
            'SELECT * FROM Patient WHERE upid = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.PatientCreateInput,
        include: Optional[types.PatientInclude] = None
    ) -> _PrismaModelT:
        """Create a new Patient record.

        Parameters
        ----------
        data
            Patient record data
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The created Patient record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Patient record from just the required fields
        patient = await Patient.prisma().create(
            data={
                # data to create a Patient record
                'fullName': 'ggciceaie',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.PatientCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Patient records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Patient record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Patient.prisma().create_many(
            data=[
                {
                    # data to create a Patient record
                    'fullName': 'bbehjachib',
                },
                {
                    # data to create a Patient record
                    'fullName': 'cadfabfehe',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.PatientWhereUniqueInput,
        include: Optional[types.PatientInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Patient record.

        Parameters
        ----------
        where
            Patient filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The deleted Patient record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        patient = await Patient.prisma().delete(
            where={
                'id': 'dgiiaaijj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.PatientWhereUniqueInput,
        include: Optional[types.PatientInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Patient record.

        Parameters
        ----------
        where
            Patient filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The found Patient record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        patient = await Patient.prisma().find_unique(
            where={
                'id': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.PatientWhereUniqueInput,
        include: Optional[types.PatientInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Patient record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Patient filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The found Patient record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        patient = await Patient.prisma().find_unique_or_raise(
            where={
                'id': 'eigcfgbif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
        include: Optional[types.PatientInclude] = None,
        order: Optional[Union[types.PatientOrderByInput, List[types.PatientOrderByInput]]] = None,
        distinct: Optional[List[types.PatientScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Patient records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Patient records returned
        skip
            Ignore the first N results
        where
            Patient filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Patient model
        order
            Order the returned Patient records by any field
        distinct
            Filter Patient records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Patient]
            The list of all Patient records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Patient records
        patients = await Patient.prisma().find_many(take=10)

        # find the first 5 Patient records ordered by the fullName field
        patients = await Patient.prisma().find_many(
            take=5,
            order={
                'fullName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
        include: Optional[types.PatientInclude] = None,
        order: Optional[Union[types.PatientOrderByInput, List[types.PatientOrderByInput]]] = None,
        distinct: Optional[List[types.PatientScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Patient record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Patient filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Patient model
        order
            Order the returned Patient records by any field
        distinct
            Filter Patient records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Patient
            The first Patient record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Patient record ordered by the gender field
        patient = await Patient.prisma().find_first(
            skip=1,
            order={
                'gender': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
        include: Optional[types.PatientInclude] = None,
        order: Optional[Union[types.PatientOrderByInput, List[types.PatientOrderByInput]]] = None,
        distinct: Optional[List[types.PatientScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Patient record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Patient filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Patient model
        order
            Order the returned Patient records by any field
        distinct
            Filter Patient records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Patient
            The first Patient record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Patient record ordered by the dateOfBirth field
        patient = await Patient.prisma().find_first_or_raise(
            skip=1,
            order={
                'dateOfBirth': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.PatientUpdateInput,
        where: types.PatientWhereUniqueInput,
        include: Optional[types.PatientInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Patient record.

        Parameters
        ----------
        data
            Patient record data specifying what to update
        where
            Patient filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The updated Patient record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        patient = await Patient.prisma().update(
            where={
                'id': 'bagcfbhiig',
            },
            data={
                # data to update the Patient record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.PatientWhereUniqueInput,
        data: types.PatientUpsertInput,
        include: Optional[types.PatientInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Patient filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Patient model

        Returns
        -------
        prisma.models.Patient
            The created or updated Patient record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        patient = await Patient.prisma().upsert(
            where={
                'id': 'cghideieh',
            },
            data={
                'create': {
                    'id': 'cghideieh',
                    'fullName': 'cadfabfehe',
                },
                'update': {
                    'fullName': 'cadfabfehe',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.PatientUpdateManyMutationInput,
        where: types.PatientWhereInput,
    ) -> int:
        """Update multiple Patient records

        Parameters
        ----------
        data
            Patient data to update the selected Patient records to
        where
            Filter to select the Patient records to update

        Returns
        -------
        int
            The total number of Patient records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Patient records
        total = await Patient.prisma().update_many(
            data={
                'demographics': Json({'biabhbdai': True})
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Patient records present in the database

        Parameters
        ----------
        select
            Select the Patient fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Patient filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PatientCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Patient.prisma().count()

        # results: prisma.types.PatientCountAggregateOutput
        results = await Patient.prisma().count(
            select={
                '_all': True,
                'contactInfo': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.PatientCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
    ) -> types.PatientCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.PatientCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PatientWhereInput] = None,
        cursor: Optional[types.PatientWhereUniqueInput] = None,
    ) -> Union[int, types.PatientCountAggregateOutput]:
        """Count the number of Patient records present in the database

        Parameters
        ----------
        select
            Select the Patient fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Patient filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PatientCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Patient.prisma().count()

        # results: prisma.types.PatientCountAggregateOutput
        results = await Patient.prisma().count(
            select={
                '_all': True,
                'notes': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PatientCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.PatientWhereInput] = None
    ) -> int:
        """Delete multiple Patient records.

        Parameters
        ----------
        where
            Optional Patient filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Patient records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Patient records
        total = await Patient.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.PatientScalarFieldKeys'],
        *,
        where: Optional['types.PatientWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PatientAvgAggregateInput'] = None,
        sum: Optional['types.PatientSumAggregateInput'] = None,
        min: Optional['types.PatientMinAggregateInput'] = None,
        max: Optional['types.PatientMaxAggregateInput'] = None,
        having: Optional['types.PatientScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PatientCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PatientScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PatientScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PatientGroupByOutput']:
        """Group Patient records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Patient fields to group records by
        where
            Patient filter to select records
        take
            Limit the maximum number of Patient records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PatientGroupByOutput]
            A list of dictionaries representing the Patient record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Patient records by createdAt values
        # and count how many records are in each group
        results = await Patient.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class VisitActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Visit]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Visit.prisma().query_raw(
            'SELECT * FROM Visit WHERE id = $1',
            'idghgaicb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Visit
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Visit.prisma().query_first(
            'SELECT * FROM Visit WHERE patientId = $1',
            'fjfddhigg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.VisitCreateInput,
        include: Optional[types.VisitInclude] = None
    ) -> _PrismaModelT:
        """Create a new Visit record.

        Parameters
        ----------
        data
            Visit record data
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The created Visit record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Visit record from just the required fields
        visit = await Visit.prisma().create(
            data={
                # data to create a Visit record
                'patientId': 'hjaecfifb',
                'clinicalData': Json({'cbbbjbfcii': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.VisitCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Visit records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Visit record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Visit.prisma().create_many(
            data=[
                {
                    # data to create a Visit record
                    'patientId': 'bbejhfidcb',
                    'clinicalData': Json({'bgeecijdgg': True}),
                },
                {
                    # data to create a Visit record
                    'patientId': 'bdiicjafbj',
                    'clinicalData': Json({'bgehebiafc': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.VisitWhereUniqueInput,
        include: Optional[types.VisitInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Visit record.

        Parameters
        ----------
        where
            Visit filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The deleted Visit record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        visit = await Visit.prisma().delete(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.VisitWhereUniqueInput,
        include: Optional[types.VisitInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Visit record.

        Parameters
        ----------
        where
            Visit filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The found Visit record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        visit = await Visit.prisma().find_unique(
            where={
                'id': 'bhghchehcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.VisitWhereUniqueInput,
        include: Optional[types.VisitInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Visit record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Visit filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The found Visit record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        visit = await Visit.prisma().find_unique_or_raise(
            where={
                'id': 'dcgchcbbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
        include: Optional[types.VisitInclude] = None,
        order: Optional[Union[types.VisitOrderByInput, List[types.VisitOrderByInput]]] = None,
        distinct: Optional[List[types.VisitScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Visit records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Visit records returned
        skip
            Ignore the first N results
        where
            Visit filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Visit model
        order
            Order the returned Visit records by any field
        distinct
            Filter Visit records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Visit]
            The list of all Visit records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Visit records
        visits = await Visit.prisma().find_many(take=10)

        # find the first 5 Visit records ordered by the visitType field
        visits = await Visit.prisma().find_many(
            take=5,
            order={
                'visitType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
        include: Optional[types.VisitInclude] = None,
        order: Optional[Union[types.VisitOrderByInput, List[types.VisitOrderByInput]]] = None,
        distinct: Optional[List[types.VisitScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Visit record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Visit filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Visit model
        order
            Order the returned Visit records by any field
        distinct
            Filter Visit records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Visit
            The first Visit record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Visit record ordered by the visitDate field
        visit = await Visit.prisma().find_first(
            skip=1,
            order={
                'visitDate': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
        include: Optional[types.VisitInclude] = None,
        order: Optional[Union[types.VisitOrderByInput, List[types.VisitOrderByInput]]] = None,
        distinct: Optional[List[types.VisitScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Visit record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Visit filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Visit model
        order
            Order the returned Visit records by any field
        distinct
            Filter Visit records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Visit
            The first Visit record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Visit record ordered by the isFollowUp field
        visit = await Visit.prisma().find_first_or_raise(
            skip=1,
            order={
                'isFollowUp': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.VisitUpdateInput,
        where: types.VisitWhereUniqueInput,
        include: Optional[types.VisitInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Visit record.

        Parameters
        ----------
        data
            Visit record data specifying what to update
        where
            Visit filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The updated Visit record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        visit = await Visit.prisma().update(
            where={
                'id': 'bdedcabahc',
            },
            data={
                # data to update the Visit record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.VisitWhereUniqueInput,
        data: types.VisitUpsertInput,
        include: Optional[types.VisitInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Visit filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Visit model

        Returns
        -------
        prisma.models.Visit
            The created or updated Visit record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        visit = await Visit.prisma().upsert(
            where={
                'id': 'ghfhiafcb',
            },
            data={
                'create': {
                    'id': 'ghfhiafcb',
                    'patientId': 'bdiicjafbj',
                    'clinicalData': Json({'bgehebiafc': True}),
                },
                'update': {
                    'patientId': 'bdiicjafbj',
                    'clinicalData': Json({'bgehebiafc': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.VisitUpdateManyMutationInput,
        where: types.VisitWhereInput,
    ) -> int:
        """Update multiple Visit records

        Parameters
        ----------
        data
            Visit data to update the selected Visit records to
        where
            Filter to select the Visit records to update

        Returns
        -------
        int
            The total number of Visit records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Visit records
        total = await Visit.prisma().update_many(
            data={
                'status': 'heejgedji'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Visit records present in the database

        Parameters
        ----------
        select
            Select the Visit fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Visit filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VisitCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Visit.prisma().count()

        # results: prisma.types.VisitCountAggregateOutput
        results = await Visit.prisma().count(
            select={
                '_all': True,
                'clinician': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.VisitCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
    ) -> types.VisitCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.VisitCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VisitWhereInput] = None,
        cursor: Optional[types.VisitWhereUniqueInput] = None,
    ) -> Union[int, types.VisitCountAggregateOutput]:
        """Count the number of Visit records present in the database

        Parameters
        ----------
        select
            Select the Visit fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Visit filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VisitCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Visit.prisma().count()

        # results: prisma.types.VisitCountAggregateOutput
        results = await Visit.prisma().count(
            select={
                '_all': True,
                'clinicalData': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.VisitCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.VisitWhereInput] = None
    ) -> int:
        """Delete multiple Visit records.

        Parameters
        ----------
        where
            Optional Visit filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Visit records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Visit records
        total = await Visit.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.VisitScalarFieldKeys'],
        *,
        where: Optional['types.VisitWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.VisitAvgAggregateInput'] = None,
        sum: Optional['types.VisitSumAggregateInput'] = None,
        min: Optional['types.VisitMinAggregateInput'] = None,
        max: Optional['types.VisitMaxAggregateInput'] = None,
        having: Optional['types.VisitScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.VisitCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.VisitScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.VisitScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.VisitGroupByOutput']:
        """Group Visit records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Visit fields to group records by
        where
            Visit filter to select records
        take
            Limit the maximum number of Visit records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.VisitGroupByOutput]
            A list of dictionaries representing the Visit record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Visit records by recommendations values
        # and count how many records are in each group
        results = await Visit.prisma().group_by(
            ['recommendations'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class FollowUpScheduleActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.FollowUpSchedule]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await FollowUpSchedule.prisma().query_raw(
            'SELECT * FROM FollowUpSchedule WHERE id = $1',
            'bjgjgibgbf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.FollowUpSchedule
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await FollowUpSchedule.prisma().query_first(
            'SELECT * FROM FollowUpSchedule WHERE patientId = $1',
            'bbbgbhfjge',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.FollowUpScheduleCreateInput,
        include: Optional[types.FollowUpScheduleInclude] = None
    ) -> _PrismaModelT:
        """Create a new FollowUpSchedule record.

        Parameters
        ----------
        data
            FollowUpSchedule record data
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The created FollowUpSchedule record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a FollowUpSchedule record from just the required fields
        followupschedule = await FollowUpSchedule.prisma().create(
            data={
                # data to create a FollowUpSchedule record
                'patientId': 'igbehcbab',
                'scheduledFor': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.FollowUpScheduleCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple FollowUpSchedule records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of FollowUpSchedule record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await FollowUpSchedule.prisma().create_many(
            data=[
                {
                    # data to create a FollowUpSchedule record
                    'patientId': 'bdadaadhag',
                    'scheduledFor': datetime.datetime.utcnow(),
                },
                {
                    # data to create a FollowUpSchedule record
                    'patientId': 'bgiggdidbf',
                    'scheduledFor': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.FollowUpScheduleWhereUniqueInput,
        include: Optional[types.FollowUpScheduleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single FollowUpSchedule record.

        Parameters
        ----------
        where
            FollowUpSchedule filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The deleted FollowUpSchedule record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        followupschedule = await FollowUpSchedule.prisma().delete(
            where={
                'id': 'caaaedabfc',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.FollowUpScheduleWhereUniqueInput,
        include: Optional[types.FollowUpScheduleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique FollowUpSchedule record.

        Parameters
        ----------
        where
            FollowUpSchedule filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The found FollowUpSchedule record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        followupschedule = await FollowUpSchedule.prisma().find_unique(
            where={
                'id': 'bigibebcib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.FollowUpScheduleWhereUniqueInput,
        include: Optional[types.FollowUpScheduleInclude] = None
    ) -> _PrismaModelT:
        """Find a unique FollowUpSchedule record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            FollowUpSchedule filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The found FollowUpSchedule record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        followupschedule = await FollowUpSchedule.prisma().find_unique_or_raise(
            where={
                'id': 'bigaiehgcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
        include: Optional[types.FollowUpScheduleInclude] = None,
        order: Optional[Union[types.FollowUpScheduleOrderByInput, List[types.FollowUpScheduleOrderByInput]]] = None,
        distinct: Optional[List[types.FollowUpScheduleScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple FollowUpSchedule records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of FollowUpSchedule records returned
        skip
            Ignore the first N results
        where
            FollowUpSchedule filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model
        order
            Order the returned FollowUpSchedule records by any field
        distinct
            Filter FollowUpSchedule records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.FollowUpSchedule]
            The list of all FollowUpSchedule records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 FollowUpSchedule records
        followupschedules = await FollowUpSchedule.prisma().find_many(take=10)

        # find the first 5 FollowUpSchedule records ordered by the scheduledFor field
        followupschedules = await FollowUpSchedule.prisma().find_many(
            take=5,
            order={
                'scheduledFor': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
        include: Optional[types.FollowUpScheduleInclude] = None,
        order: Optional[Union[types.FollowUpScheduleOrderByInput, List[types.FollowUpScheduleOrderByInput]]] = None,
        distinct: Optional[List[types.FollowUpScheduleScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single FollowUpSchedule record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FollowUpSchedule filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model
        order
            Order the returned FollowUpSchedule records by any field
        distinct
            Filter FollowUpSchedule records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FollowUpSchedule
            The first FollowUpSchedule record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FollowUpSchedule record ordered by the status field
        followupschedule = await FollowUpSchedule.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
        include: Optional[types.FollowUpScheduleInclude] = None,
        order: Optional[Union[types.FollowUpScheduleOrderByInput, List[types.FollowUpScheduleOrderByInput]]] = None,
        distinct: Optional[List[types.FollowUpScheduleScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single FollowUpSchedule record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            FollowUpSchedule filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model
        order
            Order the returned FollowUpSchedule records by any field
        distinct
            Filter FollowUpSchedule records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.FollowUpSchedule
            The first FollowUpSchedule record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second FollowUpSchedule record ordered by the reason field
        followupschedule = await FollowUpSchedule.prisma().find_first_or_raise(
            skip=1,
            order={
                'reason': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.FollowUpScheduleUpdateInput,
        where: types.FollowUpScheduleWhereUniqueInput,
        include: Optional[types.FollowUpScheduleInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single FollowUpSchedule record.

        Parameters
        ----------
        data
            FollowUpSchedule record data specifying what to update
        where
            FollowUpSchedule filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The updated FollowUpSchedule record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        followupschedule = await FollowUpSchedule.prisma().update(
            where={
                'id': 'beeifcbebf',
            },
            data={
                # data to update the FollowUpSchedule record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.FollowUpScheduleWhereUniqueInput,
        data: types.FollowUpScheduleUpsertInput,
        include: Optional[types.FollowUpScheduleInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            FollowUpSchedule filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned FollowUpSchedule model

        Returns
        -------
        prisma.models.FollowUpSchedule
            The created or updated FollowUpSchedule record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        followupschedule = await FollowUpSchedule.prisma().upsert(
            where={
                'id': 'bgcigfahea',
            },
            data={
                'create': {
                    'id': 'bgcigfahea',
                    'patientId': 'bgiggdidbf',
                    'scheduledFor': datetime.datetime.utcnow(),
                },
                'update': {
                    'patientId': 'bgiggdidbf',
                    'scheduledFor': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.FollowUpScheduleUpdateManyMutationInput,
        where: types.FollowUpScheduleWhereInput,
    ) -> int:
        """Update multiple FollowUpSchedule records

        Parameters
        ----------
        data
            FollowUpSchedule data to update the selected FollowUpSchedule records to
        where
            Filter to select the FollowUpSchedule records to update

        Returns
        -------
        int
            The total number of FollowUpSchedule records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all FollowUpSchedule records
        total = await FollowUpSchedule.prisma().update_many(
            data={
                'notes': 'bcejgaggif'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
    ) -> int:
        """Count the number of FollowUpSchedule records present in the database

        Parameters
        ----------
        select
            Select the FollowUpSchedule fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FollowUpSchedule filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FollowUpScheduleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FollowUpSchedule.prisma().count()

        # results: prisma.types.FollowUpScheduleCountAggregateOutput
        results = await FollowUpSchedule.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.FollowUpScheduleCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
    ) -> types.FollowUpScheduleCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.FollowUpScheduleCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.FollowUpScheduleWhereInput] = None,
        cursor: Optional[types.FollowUpScheduleWhereUniqueInput] = None,
    ) -> Union[int, types.FollowUpScheduleCountAggregateOutput]:
        """Count the number of FollowUpSchedule records present in the database

        Parameters
        ----------
        select
            Select the FollowUpSchedule fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            FollowUpSchedule filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.FollowUpScheduleCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await FollowUpSchedule.prisma().count()

        # results: prisma.types.FollowUpScheduleCountAggregateOutput
        results = await FollowUpSchedule.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.FollowUpScheduleCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.FollowUpScheduleWhereInput] = None
    ) -> int:
        """Delete multiple FollowUpSchedule records.

        Parameters
        ----------
        where
            Optional FollowUpSchedule filter to find the records to be deleted

        Returns
        -------
        int
            The total number of FollowUpSchedule records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all FollowUpSchedule records
        total = await FollowUpSchedule.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.FollowUpScheduleScalarFieldKeys'],
        *,
        where: Optional['types.FollowUpScheduleWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.FollowUpScheduleAvgAggregateInput'] = None,
        sum: Optional['types.FollowUpScheduleSumAggregateInput'] = None,
        min: Optional['types.FollowUpScheduleMinAggregateInput'] = None,
        max: Optional['types.FollowUpScheduleMaxAggregateInput'] = None,
        having: Optional['types.FollowUpScheduleScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.FollowUpScheduleCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.FollowUpScheduleScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.FollowUpScheduleScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.FollowUpScheduleGroupByOutput']:
        """Group FollowUpSchedule records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar FollowUpSchedule fields to group records by
        where
            FollowUpSchedule filter to select records
        take
            Limit the maximum number of FollowUpSchedule records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.FollowUpScheduleGroupByOutput]
            A list of dictionaries representing the FollowUpSchedule record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group FollowUpSchedule records by id values
        # and count how many records are in each group
        results = await FollowUpSchedule.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models